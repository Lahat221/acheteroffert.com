# 📸 PLACEZ VOTRE LOGO ICI

## ⚠️ IMPORTANT

Pour que le logo s'affiche sur le site, vous DEVEZ :

1. **Copier votre fichier image** dans ce dossier (`frontend/public/`)

2. **Nommer le fichier exactement** : 
   - `logo.jpeg` (recommandé)
   - ou `logo.jpg`
   - ou `logo.png`

3. **Emplacement exact** :
   ```
   frontend/public/logo.jpeg
   ```

## 📋 Étapes

1. Trouvez votre fichier image du logo
2. Copiez-le dans ce dossier (`frontend/public/`)
3. Renommez-le en `logo.jpeg` (ou `logo.jpg`, `logo.png`)
4. Rechargez la page du navigateur (F5)

## ✅ Vérification

Une fois l'image placée, elle devrait s'afficher automatiquement dans le header du site.

## 🔍 Si ça ne fonctionne pas

- Vérifiez que le fichier s'appelle exactement `logo.jpeg` (sensible à la casse)
- Vérifiez que le fichier est bien dans `frontend/public/` (pas dans `public/` à la racine)
- Vérifiez le format : .jpeg, .jpg ou .png
- Rechargez la page (F5)
- Redémarrez le serveur Next.js si nécessaire

