# 📸 Logo AcheterOffert.com

## 📍 Emplacement du logo

Placez votre fichier image du logo dans ce dossier (`frontend/public/`) avec le nom :
- `logo.jpeg` (recommandé)
- ou `logo.png`
- ou `logo.svg`

## ✅ Vérification

Une fois l'image placée, le composant `Logo.tsx` l'utilisera automatiquement.

Le logo sera accessible via : `/logo.jpeg` (ou le nom que vous avez choisi)

## 📝 Note

Si vous utilisez un autre nom de fichier, modifiez le composant `Logo.tsx` :
```tsx
src="/votre-nom-de-fichier.jpeg"
```

